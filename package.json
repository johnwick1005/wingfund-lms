{
  "name": "wingfund-lms",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"API,UI\" -c \"magenta,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:java": "concurrently -n \"JAVA,UI\" -c \"magenta,cyan\" \"mvn spring-boot:run -f java-backend/pom.xml\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:java": "mvn package -f java-backend/pom.xml -q"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
